<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              xmlns:wheel="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:gravity="center_vertical"
              android:orientation="horizontal"
              android:paddingLeft="0dp"
              android:paddingRight="@dimen/material_size_atom">

    <cn.byk.pandora.matdialog.progress.ProgressWheel
        android:id="@+id/progress"
        android:layout_width="@dimen/icon_size_huge"
        android:layout_height="@dimen/icon_size_huge"
        wheel:matProg_barColor="@color/blue"
        wheel:matProg_progressIndeterminate="true"/>

    <TextView
        android:id="@+id/loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="1"
        android:paddingLeft="@dimen/material_size_atom"
        android:textColor="@color/gray"
        android:textSize="@dimen/font_size_sub_title"
        tools:text="Loading..."/>

</LinearLayout>